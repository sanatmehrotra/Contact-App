<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My-contacts</title>
    <link rel="stylesheet" href="/styles/newContact.css">
</head>

<body>
    <form action="/contacts/editContact/<%= contact._id%>" method="post">
    <div class="new-contact">
        <header>
            <h1>edit-contact</h1>
        </header>

         <div class="form">

            <div class="input">
             <input type="text" name="name" placeholder="Name" required value= <%= contact.name %> >
            </div>
           <div class="input">
            <input type="email" name="email" placeholder="Email" required value= <%= contact.email %>>
           </div>
           <div class="input">
             <input type="text" name="phone" placeholder="Phone" required value=<%= contact.phone %>>
           </div>

            <button type="submit">Save</button>
         </div>
            
    </div>
</form>
</body>
</html>